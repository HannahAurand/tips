<h1 class="storiesPageHeadline">Stories</h1>

<% @blogs.each do |blog| %>
    <h2><%= link_to blog.title, blog %></h2>
    <p><%= truncate(blog.story, :length => 200) %></p>
<% end %>

<%= link_to "New Story", new_blog_path %>
